server:
  port: 8765
spring:
  application:
    name: api-gateway
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
zuul:
  prefix: /api
  routes:
    products:
      path: /products/**
      serviceId: products-service
      stripPrefix: false
    recipes:
      path: /recipes/**
      serviceId: recipes-service
      stripPrefix: false